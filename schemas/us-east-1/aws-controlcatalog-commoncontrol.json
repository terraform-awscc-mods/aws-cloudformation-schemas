{
  "typeName" : "AWS::ControlCatalog::CommonControl",
  "description" : "Returns information about a common control in the AWS Control Catalog.",
  "properties" : {
    "Arn" : {
      "type" : "string",
      "description" : "The Amazon Resource Name (ARN) that identifies the common control.",
      "minLength" : 41,
      "maxLength" : 2048,
      "pattern" : "^arn:(aws(?:[-a-z]*)?):controlcatalog:::common-control/[0-9a-z]+$"
    },
    "CommonControlId" : {
      "type" : "string",
      "description" : "The unique identifier of the common control.",
      "pattern" : "^[0-9a-z]+$"
    },
    "Name" : {
      "type" : "string",
      "description" : "The name of the common control."
    },
    "Description" : {
      "type" : "string",
      "description" : "The description of the common control."
    },
    "Domain" : {
      "type" : "object",
      "description" : "The domain that the common control belongs to.",
      "properties" : {
        "Arn" : {
          "type" : "string",
          "description" : "The Amazon Resource Name (ARN) of the related domain."
        },
        "Name" : {
          "type" : "string",
          "description" : "The name of the related domain."
        }
      },
      "additionalProperties" : false
    },
    "Objective" : {
      "type" : "object",
      "description" : "The objective that the common control belongs to.",
      "properties" : {
        "Arn" : {
          "type" : "string",
          "description" : "The Amazon Resource Name (ARN) of the related objective."
        },
        "Name" : {
          "type" : "string",
          "description" : "The name of the related objective."
        }
      },
      "additionalProperties" : false
    },
    "CreateTime" : {
      "type" : "string",
      "description" : "The time when the common control was created.",
      "format" : "date-time"
    },
    "LastUpdateTime" : {
      "type" : "string",
      "description" : "The time when the common control was most recently updated.",
      "format" : "date-time"
    }
  },
  "additionalProperties" : false,
  "readOnlyProperties" : [ "/properties/Arn", "/properties/CommonControlId", "/properties/Name", "/properties/Description", "/properties/Domain", "/properties/Objective", "/properties/CreateTime", "/properties/LastUpdateTime" ],
  "primaryIdentifier" : [ "/properties/Arn" ],
  "additionalIdentifiers" : [ [ "/properties/CommonControlId" ] ],
  "tagging" : {
    "taggable" : false
  },
  "handlers" : {
    "read" : {
      "permissions" : [ "controlcatalog:ListCommonControls" ]
    },
    "list" : {
      "permissions" : [ "controlcatalog:ListCommonControls" ]
    }
  }
}