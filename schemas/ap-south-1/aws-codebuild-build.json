{
  "typeName" : "AWS::CodeBuild::Build",
  "description" : "Represents an AWS CodeBuild build, which is a single execution of a build project.",
  "properties" : {
    "Arn" : {
      "type" : "string",
      "description" : "The Amazon Resource Name (ARN) of the build.",
      "minLength" : 1
    },
    "Id" : {
      "type" : "string",
      "description" : "The unique ID for the build (format: projectName:buildId).",
      "minLength" : 1
    },
    "ProjectName" : {
      "type" : "string",
      "description" : "The name of the CodeBuild build project.",
      "minLength" : 1
    },
    "BuildNumber" : {
      "type" : "integer",
      "description" : "The number of the build."
    },
    "BuildStatus" : {
      "type" : "string",
      "description" : "The current status of the build.",
      "enum" : [ "SUCCEEDED", "FAILED", "FAULT", "TIMED_OUT", "IN_PROGRESS", "STOPPED" ]
    },
    "BuildComplete" : {
      "type" : "boolean",
      "description" : "Whether the build is complete."
    },
    "CurrentPhase" : {
      "type" : "string",
      "description" : "The current build phase."
    },
    "StartTime" : {
      "type" : "string",
      "format" : "date-time",
      "description" : "When the build process started, in ISO 8601 format."
    },
    "EndTime" : {
      "type" : "string",
      "format" : "date-time",
      "description" : "When the build process ended, in ISO 8601 format."
    },
    "ServiceRole" : {
      "type" : "string",
      "description" : "The name of a service role used for this build.",
      "minLength" : 1
    },
    "EncryptionKey" : {
      "type" : "string",
      "description" : "The KMS key used to encrypt the build output artifacts.",
      "minLength" : 1
    },
    "TimeoutInMinutes" : {
      "type" : "integer",
      "description" : "How long, in minutes, from 5 to 2160 (36 hours), for CodeBuild to wait before timing out the build."
    },
    "QueuedTimeoutInMinutes" : {
      "type" : "integer",
      "description" : "The number of minutes a build is allowed to be queued before it times out."
    },
    "Initiator" : {
      "type" : "string",
      "description" : "The entity that started the build."
    }
  },
  "additionalProperties" : false,
  "createOnlyProperties" : [ "/properties/ProjectName" ],
  "readOnlyProperties" : [ "/properties/Arn", "/properties/Id", "/properties/BuildNumber", "/properties/BuildStatus", "/properties/BuildComplete", "/properties/CurrentPhase", "/properties/StartTime", "/properties/EndTime", "/properties/ServiceRole", "/properties/EncryptionKey", "/properties/TimeoutInMinutes", "/properties/QueuedTimeoutInMinutes", "/properties/Initiator" ],
  "primaryIdentifier" : [ "/properties/Arn" ],
  "additionalIdentifiers" : [ [ "/properties/Id" ] ],
  "tagging" : {
    "taggable" : false
  },
  "handlers" : {
    "read" : {
      "permissions" : [ "codebuild:BatchGetBuilds" ]
    },
    "list" : {
      "permissions" : [ "codebuild:ListBuilds", "codebuild:BatchGetBuilds" ]
    }
  }
}