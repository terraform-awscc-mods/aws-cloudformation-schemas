{
  "typeName" : "AWS::QuickSight::AssetBundleExportJob",
  "description" : "Represents a QuickSight asset bundle export job.",
  "properties" : {
    "Arn" : {
      "type" : "string",
      "description" : "The Amazon Resource Name (ARN) for the export job."
    },
    "AssetBundleExportJobId" : {
      "type" : "string",
      "description" : "The ID of the export job.",
      "minLength" : 1,
      "maxLength" : 512,
      "pattern" : "^[\\w\\-]+$"
    },
    "AwsAccountId" : {
      "type" : "string",
      "description" : "The ID of the Amazon Web Services account to export assets from.",
      "minLength" : 12,
      "maxLength" : 12,
      "pattern" : "^[0-9]{12}$"
    },
    "ResourceArns" : {
      "type" : "array",
      "description" : "An array of resource ARNs to export.",
      "items" : {
        "type" : "string"
      },
      "minItems" : 1,
      "maxItems" : 100
    },
    "IncludeAllDependencies" : {
      "type" : "boolean",
      "description" : "A Boolean that determines whether all dependencies of each resource ARN are recursively exported with the job."
    },
    "ExportFormat" : {
      "type" : "string",
      "description" : "The export data format.",
      "enum" : [ "CLOUDFORMATION_JSON", "QUICKSIGHT_JSON" ]
    },
    "IncludePermissions" : {
      "type" : "boolean",
      "description" : "A Boolean that determines whether all permissions for each resource ARN are exported with the job."
    },
    "IncludeTags" : {
      "type" : "boolean",
      "description" : "A Boolean that determines whether all tags for each resource ARN are exported with the job."
    },
    "IncludeFolderMemberships" : {
      "type" : "boolean",
      "description" : "A Boolean that determines if the exported asset carries over information about the folders that the asset is a member of."
    },
    "IncludeFolderMembers" : {
      "type" : "string",
      "description" : "A setting that indicates whether you want to include folder assets.",
      "enum" : [ "RECURSE", "ONE_LEVEL", "NONE" ]
    },
    "JobStatus" : {
      "type" : "string",
      "description" : "Indicates the status of a job through its queuing and execution.",
      "enum" : [ "QUEUED_FOR_IMMEDIATE_EXECUTION", "IN_PROGRESS", "SUCCESSFUL", "FAILED" ]
    },
    "CreatedTime" : {
      "type" : "string",
      "description" : "The time that the export job was created.",
      "format" : "date-time"
    }
  },
  "required" : [ "AssetBundleExportJobId", "ResourceArns", "ExportFormat" ],
  "additionalProperties" : false,
  "createOnlyProperties" : [ "/properties/AssetBundleExportJobId", "/properties/AwsAccountId", "/properties/ExportFormat", "/properties/IncludeAllDependencies", "/properties/IncludeFolderMembers", "/properties/IncludeFolderMemberships", "/properties/IncludePermissions", "/properties/IncludeTags", "/properties/ResourceArns" ],
  "readOnlyProperties" : [ "/properties/Arn", "/properties/JobStatus", "/properties/CreatedTime" ],
  "primaryIdentifier" : [ "/properties/Arn" ],
  "additionalIdentifiers" : [ [ "/properties/AssetBundleExportJobId" ] ],
  "tagging" : {
    "taggable" : false
  },
  "handlers" : {
    "read" : {
      "permissions" : [ "quicksight:DescribeAssetBundleExportJob" ]
    },
    "list" : {
      "permissions" : [ "quicksight:ListAssetBundleExportJobs" ]
    }
  }
}