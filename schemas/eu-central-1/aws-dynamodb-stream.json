{
  "typeName" : "AWS::DynamoDB::Stream",
  "description" : "Resource schema for a DynamoDB Stream, providing read-only visibility into stream metadata.",
  "properties" : {
    "StreamArn" : {
      "type" : "string",
      "description" : "The Amazon Resource Name (ARN) for the stream.",
      "minLength" : 37,
      "maxLength" : 1024
    },
    "TableName" : {
      "type" : "string",
      "description" : "The DynamoDB table with which the stream is associated.",
      "minLength" : 3,
      "maxLength" : 255,
      "pattern" : "^[a-zA-Z0-9_.-]+$"
    },
    "StreamLabel" : {
      "type" : "string",
      "description" : "A timestamp, in ISO 8601 format, for this stream."
    },
    "StreamStatus" : {
      "type" : "string",
      "description" : "Indicates the current status of the stream.",
      "enum" : [ "ENABLING", "ENABLED", "DISABLING", "DISABLED" ]
    },
    "StreamViewType" : {
      "type" : "string",
      "description" : "Indicates the format of the records within this stream.",
      "enum" : [ "KEYS_ONLY", "NEW_IMAGE", "OLD_IMAGE", "NEW_AND_OLD_IMAGES" ]
    },
    "CreationRequestDateTime" : {
      "type" : "string",
      "description" : "The date and time when the request to create this stream was issued."
    },
    "KeySchema" : {
      "type" : "array",
      "description" : "The key attribute(s) of the stream's DynamoDB table.",
      "items" : {
        "type" : "object",
        "properties" : {
          "AttributeName" : {
            "type" : "string",
            "description" : "The name of a key attribute.",
            "minLength" : 1,
            "maxLength" : 255
          },
          "KeyType" : {
            "type" : "string",
            "description" : "The role that this key attribute will assume.",
            "enum" : [ "HASH", "RANGE" ]
          }
        },
        "required" : [ "AttributeName", "KeyType" ],
        "additionalProperties" : false
      },
      "minItems" : 1,
      "maxItems" : 2
    }
  },
  "createOnlyProperties" : [ "/properties/StreamViewType", "/properties/TableName" ],
  "readOnlyProperties" : [ "/properties/StreamArn", "/properties/StreamLabel", "/properties/StreamStatus", "/properties/CreationRequestDateTime", "/properties/KeySchema" ],
  "additionalProperties" : false,
  "primaryIdentifier" : [ "/properties/StreamArn" ],
  "additionalIdentifiers" : [ [ "/properties/TableName", "/properties/StreamLabel" ] ],
  "tagging" : {
    "taggable" : false
  },
  "handlers" : {
    "read" : {
      "permissions" : [ "dynamodb:DescribeStream" ]
    },
    "list" : {
      "permissions" : [ "dynamodb:ListStreams" ]
    }
  }
}